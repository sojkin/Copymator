[project]
name = "copymator"
version = "0.1.0"
description = "Konsolowe narzędzie do kopiowania zdjęć z kart pamięci z możliwością rozbudowy o GUI."
requires-python = ">=3.10"
dependencies = [
  "pillow>=10.0.0",
]

[project.scripts]
copymator = "copymator.cli:main"

